#!/bin/bash
# this gets run from within the nginx Docker container, at /usr/local/share/envsub

source ./env-default.sh \
&& ./envsub < ./nginx.conf.template > /etc/nginx/nginx.conf \
# && cat /etc/nginx/nginx.conf \
&& nginx -g 'daemon off;'
